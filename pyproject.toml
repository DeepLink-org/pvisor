[project]
name = "persisting"
version = "0.3.0"
description = "Reviewable Agent execution boundary"
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
authors = [
    {name = "reiase"}
]

[project.urls]
Homepage = "https://deeplink-org.github.io/Persisting/"
Documentation = "https://deeplink-org.github.io/Persisting/"
Repository = "https://github.com/DeepLink-org/Persisting"
Issues = "https://github.com/DeepLink-org/Persisting/issues"

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]

[tool.setuptools.packages.find]
include = ["persisting*"]

[build-system]
requires = ["setuptools>=77"]
build-backend = "build_backend"
backend-path = ["scripts/packaging"]

[tool.cibuildwheel]
build = "cp312-*"
# Nightly/PyPI only publish manylinux + macOS wheels; musllinux is unused
# and its before-all still assumes yum.
skip = "*-musllinux_*"
build-frontend = "build"

[tool.cibuildwheel.linux]
archs = ["x86_64"]
manylinux-x86_64-image = "manylinux_2_28"
before-all = [
    "yum install -y curl glibc-static",
    "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --profile minimal --default-toolchain stable",
]
# rustc libstd and libkrun's virtiofs passthrough need statx (glibc 2.28)
# and copy_file_range (glibc 2.27). Targeting glibc 2.17 cannot satisfy
# those refs, so the wheel tag is manylinux_2_28.
environment = { PATH = "$HOME/.cargo/bin:$PATH" }

[tool.cibuildwheel.macos]
archs = ["arm64"]
environment = { MACOSX_DEPLOYMENT_TARGET = "11.0" }
repair-wheel-command = "cp {wheel} {dest_dir}"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]

[tool.pytest.ini_options]
pythonpath = ["."]
