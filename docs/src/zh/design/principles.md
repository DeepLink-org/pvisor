# 设计原则

PolicyVisor 将策略意图、运行时控制和可审查的结果关联起来。以下原则要求这三者的关系始终明确。

## 边界必须明确

pVisor 描述实际安装的执行边界。它不会把缺失的控制默认为更强的保证。

## 暂存文件先审查再应用

启用 `--stage` 后，工作区文件改动在显式应用前保留在暂存区。审查应发生在这些改动进入项目之前。这并不意味着远程 API 调用或其他外部副作用也可以撤销。

## 结果必须带着证据走

汇总应该能回到产生它的 Run。只有在再次检查之后仍保留 lineage，它才真正有用。

## Capture 保持可选

pVisor 可以不打开模型流量捕获而运行。打开之后，capture 是写入同一次 Run 的窄交接，不是第二个产品。

## 可移植数据优先于特权查看器

Run 记录应该可以通过 CLI 和公开格式检查。Web 界面可以改善发现，但不应成为恢复答案的唯一方式。

参见[系统概览](index.md)和[路线图](../development/roadmap.md)。
